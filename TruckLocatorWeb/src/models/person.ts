export interface IPerson {
    address: string;
    birthDate: Date;
    birthNumber: string;
    firstName: string;
    lastName: string;
    idNumber: string;
    specialisation: string;
    tracks: string;
    photo: string;
}
