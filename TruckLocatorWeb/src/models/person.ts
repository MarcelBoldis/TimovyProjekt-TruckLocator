export interface IPerson {
    firstName: string;
    lastName: string;
    birthNumber: string;
    idNumber: string;
    birthDate: Date;
    specialisation: number;
    address: string;
    photo: string;
}
